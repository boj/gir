[server]
server_address: colol.oneoaks.net
server_port: 6667
# If the server requires a password to join, set it here
server_password = cololirc
default_channel: #lunix

[bot]
nickname: gir
# Nickname of the person administering the bot
admin: bojo

# The following are advanced settings and probably do not
# need to be changed.
[bot_config]
module_dir = modules
module_exec = main
bot_data_file = data/gir_data.dat